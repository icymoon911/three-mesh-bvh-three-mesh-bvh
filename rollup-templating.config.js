// Template generation has been removed. All cast/ and iteration utilities now
// use runtime branching based on the BVH's indirect flag instead of build-time
// preprocessing. This file is retained as a no-op so that existing build scripts
// referencing it continue to work without modification.
export default [];
